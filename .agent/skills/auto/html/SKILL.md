---
name: html
description: "Html for ebanking. 18 conventions, 29 fixes, 1 error→fix pairs."
domain: html
triggers:
  - glob: "**/*.html"
autoGenerated: true
enabled: true
---

# Html

Auto-compiled from **87 real patterns** in **ebanking**. This skill is auto-routed to agents when working on html files.

## 🔧 Problem Playbooks

### Known Error → Fix

| Error Pattern | Fix | Seen |
|-------|-----|------|
| `-     Logger.log("doGet Error: " + e.message);` | Fixed null crash in HtmlService | 2x |

### problem-fix in index.html
-     <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
+     <script src="https://code.jquery.com/jquery-3.7.1.min.js" onerror="alert('Không thể tải jQuery từ CDN. Vui lòng kiểm tra internet!')"></script>
-     <script src="app.js"></script>
+     <script src="app.js?v=2"></script>

📌 IDE AST Context: Modified symbols likely include [html]

**Actionable Steps:**
1. Modified 1 files

### Fixed null crash in AppCache — wraps unsafe operation in error boundary
-             // 2. Tìm kiếm thời gian thực, không phân biệt HOA/thường
+             // Nút Xóa Lọc Ngày
-             $('#tblKH_filter input').off('keyup').on('keyup', function() {
+             $('#btnClearDateAdmin').off('click').on('click', function() {
-                 if (dtTable) dtTable.search($(this).val(), false, true, false).draw();
+                 var fp = document.getElementById('

**Actionable Steps:**
1. Modified 1 files
2. identifier: HOA
3. identifier: AppCache
4. identifier: Loading
5. identifier: Admin

### Fixed null crash in GIAN — wraps unsafe operation in error boundary
-                         <input type="text" id="filterDateAdmin" class="form-control" placeholder="Lọc Từ - Đến ngày">
+                         <input type="text" id="filterDateAdmin" class="form-control" placeholder="Lọc Từ — Đến ngày (tự động)">
-                     </div>
+                         <button class="btn btn-outline-secondary" type="button" id="btnClearDateAdmin" title="Xóa bộ lọ

**Actionable Steps:**
1. Modified 1 files
2. identifier: GIAN
3. identifier: CCCD
4. identifier: GPKD
5. identifier: THAO

### Fixed null crash in Admin — wraps unsafe operation in error boundary
-             try { initDashboard(); } catch(e) { console.warn("initDashboard init fail:", e); }
+             $('#staffBottomNav').hide(); // Admin không cần menu của Nhân viên
-             showView('view-dashboard');
+             try { initDashboard(); } catch(e) { console.warn("initDashboard init fail:", e); }
-         } else {
+             showView('view-dashboard');
-             $('#staf

**Actionable Steps:**
1. Modified 1 files
2. identifier: Admin
3. identifier: Start
4. identifier: AppState
5. identifier: AppCache

### Fixed null crash in Flatpickr — wraps unsafe operation in error boundary
-     function initDashboard() {
+     // Hàm tạo badge trạng thái hồ sơ
-         try {
+     function getStatusBadge(status) {
-             // 1. Flatpickr cho bộ lọc khoảng thời gian — dùng YYYY-MM-DD để so sánh chính xác
+         var map = {
-             var fpAdminEl = document.getElementById('filterDateAdmin');
+             'Đã xác minh':  '<span class="badge bg-success">Đã xác minh</spa

**Actionable Steps:**
1. Modified 1 files
2. identifier: Thi
3. identifier: Flatpickr
4. identifier: YYYY
5. identifier: Date

### Fixed null crash in Camera — wraps unsafe operation in error boundary
-         var uploadIds = ['img_truoc', 'img_sau', 'img_dkkd', 'img_qr'];
+         var uploadIds = ['img_truoc', 'img_sau', 'img_dkkd', 'img_qr', 'img_thuchien'];
-     }
+ 
- 
+         // Camera inputs nhét ảnh chụp vào đúng slot của file input gốc
-     var moTaiKhoan_checkDuplicateRealtime = function(input) {
+         var camMap = {
-         var val = input.value.trim();
+             'cam_

**Actionable Steps:**
1. Modified 1 files
2. identifier: Camera
3. identifier: Object
4. identifier: Copy
5. identifier: DataTransfer

### Fixed null crash in Camera — wraps unsafe operation in error boundary
-                     <button class="btn btn-outline-primary" type="button" onclick="openCamera('img_truoc')" title="Chụp Camera"><i class='bx bx-camera'></i></button>
+                     <input type="file" id="cam_truoc" accept="image/*" capture="environment" class="d-none">
-                 </div>
+                     <label for="cam_truoc" class="btn btn-outline-primary" title="Chụp Camera"

**Actionable Steps:**
1. Modified 1 files
2. identifier: Camera
3. identifier: CCCD
4. identifier: KKD
5. identifier: Thanh

### Fixed null crash in String — wraps unsafe operation in error boundary
-             showAlert('Lỗi Dashboard', 'Không thể hiển thị dữ liệu thống kê. Vui lòng kiểm tra console.', 'error');
+             var printErr = (err && err.message) ? err.message : String(err);
-         }
+             if (err && err.stack) printErr += " | Stack: " + err.stack.toString().substring(0, 100);
-     }
+             showAlert('Lỗi Dashboard', 'Lỗi: ' + printErr, 'error');
- 
+     

**Actionable Steps:**
1. Modified 1 files
2. identifier: String
3. identifier: Stack
4. identifier: Dashboard
5. identifier: Thu

### Fixed null crash in Modal — wraps unsafe operation in error boundary
- <!-- Modal Camera -->
+ <!-- Modal Cắt Ảnh (Cropper) -->
- <div class="modal fade" id="cameraModal" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
+ <div class="modal fade" id="cropModal" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
-   <div class="modal-dialog modal-fullscreen-sm-down modal-lg">
+   <div class="modal-dialog modal-lg modal-dialog-centered">
-     <div

**Actionable Steps:**
1. Modified 1 files
2. identifier: Modal
3. identifier: Cropper
4. identifier: Close
5. identifier: CONFIGURED

### Fixed null crash in AppState — cleans up event listeners to prevent memory leaks
- 
+         $('#edit_ten_dang_nhap').val((row['Tên đăng nhập'] || '').toString().replace(/^'/, ''));
-         if (AppState.user && AppState.user.role === 'Admin') {
+ 
-             $('#btnSaveEdit').hide();
+         if (AppState.user && AppState.user.role === 'Admin') {
-             $('#frmEditCustomer input').prop('readonly', true);
+             $('#btnSaveEdit').hide();
-             var f

**Actionable Steps:**
1. Modified 1 files
2. identifier: AppState
3. identifier: Admin
4. identifier: Keep
5. identifier: View

### Fixed null crash in CCCD — wraps unsafe operation in error boundary
-         </div>
+             <div class="col-12 col-md-6 initially-hidden" id="div_ten_dang_nhap">
- 
+                 <label class="form-label fw-semibold">Tên đăng nhập <span class="text-muted small fw-normal">(Tuỳ chọn cho HKD)</span></label>
-         <!-- Phần C: Hồ sơ Cứng -->
+                 <input type="text" class="form-control" id="ten_dang_nhap" placeholder="Tên đăng nhập">
-      

**Actionable Steps:**
1. Modified 1 files
2. identifier: HKD
3. identifier: CCCD
4. identifier: Camera
5. identifier: KKD

### Fixed null crash in Date — wraps unsafe operation in error boundary
-                             var from = selectedDates[0].setHours(0,0,0,0);
+                             var from = new Date(selectedDates[0]).setHours(0,0,0,0);
-                             var to   = selectedDates[1].setHours(23,59,59,999);
+                             var to   = new Date(selectedDates[1]).setHours(23,59,59,999);
-                                 var raw = window._adminAllDa

**Actionable Steps:**
1. Modified 1 files
2. identifier: Date
3. identifier: HOA
4. identifier: AppCache
5. identifier: Loading

### Fixed null crash in Theo — wraps unsafe operation in error boundary
- <div class="row g-4">
+ <!-- Biểu đồ Tiến độ Theo Tháng -->
-     <div class="col-12 col-xl-8">
+ <div class="row g-4 mb-4">
-         <div class="glass-card p-3 p-md-4 h-100 flex-grow-1">
+     <div class="col-12">
-             <div class="d-flex justify-content-between align-items-center mb-3 flex-wrap gap-2">
+         <div class="glass-card p-3 p-md-4">
-                 <h5 class="fw-bold 

**Actionable Steps:**
1. Modified 1 files
2. identifier: Theo
3. identifier: Kho
4. identifier: GIAN
5. identifier: CCCD

### Fixed null crash in Crop — wraps unsafe operation in error boundary
-                 <div class="progress-bar progress-bar-striped progress-bar-animated bg-success" id="compress-progress-bar" role="progressbar" title="Tiến trình nén ảnh" aria-label="Tiến trình nén ảnh" style="width: 0%"></div>
+                 <div class="progress-bar progress-bar-striped progress-bar-animated bg-success progress-bar-zero" id="compress-progress-bar" role="progressbar" title="Tiế

**Actionable Steps:**
1. Modified 1 files
2. identifier: Crop
3. identifier: Compress
4. identifier: Modal
5. identifier: Camera

### Fixed null crash in Camera — wraps unsafe operation in error boundary
-                     <input class="form-control" type="file" id="img_truoc" aria-label="Tải lên ảnh CCCD mặt trước" title="Tải lên ảnh CCCD mặt trước" accept="image/jpeg, image/png" required>
+                     <input class="form-control" type="file" id="img_truoc" accept="image/*" required>
-                     <button class="btn btn-outline-primary" type="button" aria-label="Mở Camera chụp 

**Actionable Steps:**
1. Modified 1 files
2. identifier: Camera
3. identifier: CCCD
4. identifier: KKD
5. identifier: Thanh

### Fixed null crash in CCCD — wraps unsafe operation in error boundary
-                 var cccd = (d['Số CCCD'] || d['Số DKKD'] || '').toString().replace(/^'/, '');
+                 var cccd = (d['Số CCCD'] || '').toString().replace(/^'/, '');
-                 var sdt = (d['Số điện thoại'] || '').toString().replace(/^'/, '');
+                 var dkkd = (d['Số ĐKKD'] || d['Số GP ĐKKD'] || '').toString().replace(/^'/, '');
-                 var stk = (d['Số TK'] 

**Actionable Steps:**
1. Modified 1 files
2. identifier: CCCD
3. identifier: KKD
4. identifier: Chi
5. identifier: DataTable

### Fixed null crash in Math — parallelizes async operations for speed
-                     var MAX_W = 1200, MAX_H = 1200;
+                     try {
-                     var w = img.width, h = img.height;
+                         var MAX_W = 1200, MAX_H = 1200;
-                     if (w > MAX_W) { h = Math.round(h * MAX_W / w); w = MAX_W; }
+                         var w = img.width, h = img.height;
-                     if (h > MAX_H) { w = Math.round(w * M

**Actionable Steps:**
1. Modified 1 files
2. identifier: Math
3. identifier: Promise
4. identifier: AppState
5. identifier: Swal

### Fixed null crash in Image — parallelizes async operations for speed
-         var tasks = files.map(function(id) {
+         
-             return new Promise(function(res) {
+         function fastCompress(file, callback) {
-                 var input = document.getElementById(id);
+             if(!file) return callback(null);
-                 var f = input.files[0] || input._customFile;
+             var img = new Image();
-                 if(!f) return res()

**Actionable Steps:**
1. Modified 1 files
2. identifier: Image
3. identifier: FileReader
4. identifier: Math
5. identifier: Promise

### problem-fix in jsApp.html
-         if (stk.length > 6 && stk.startsWith('380200')) {
+         if (stk.length > 7 && stk.startsWith('3800200')) {
-             stk = stk.substring(6);
+             stk = stk.substring(7);
- </script>
+ 
- 
+ $(document).ready(function() {
+     $('#frmEditCustomer').off('submit').on('submit', function(e) {
+         e.preventDefault();
+         var data = {
+             id: $('#edit_id'

**Actionable Steps:**
1. Modified 1 files

### Fixed null crash in Excel — wraps unsafe operation in error boundary
-                 language: { url: "https://cdn.datatables.net/plug-ins/1.13.6/i18n/vi.json" },
+                 dom: "<'row mb-2'<'col-sm-1

... [Truncated — see individual observations for full content]
